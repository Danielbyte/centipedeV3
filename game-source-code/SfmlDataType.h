// Sfml data types definitions
#include <SFML/Graphics.hpp>
#ifndef SFMLDATATYPES_H
#define SFMLDATATYPES_H

using VideoMode = sf::VideoMode;
using RenderWindow = sf::RenderWindow;
using Font = sf::Font;
using Text = sf::Text;
using Texture = sf::Texture;
using Color = sf::Color;
using Event = sf::Event;
using Keyboard = sf::Keyboard::Key;
using Sprite = sf::Sprite;
using vector2f = sf::Vector2f;
using CircleShape = sf::CircleShape;
using RectangleShape = sf::RectangleShape;
using intRect = sf::IntRect;
using FloatRect = sf::FloatRect;
using RenderTarget = sf::RenderTarget;
using Clock = sf::Clock;

#endif // SFMLDATATYPES_H

